export const metadata = {
  title: "Admin — bimimport",
  description: "Cohost dashboard: pipeline steps, sources, and debug entry points",
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
