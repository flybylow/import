/**
 * Text appended to each template-seeded row’s `timeline:message` (plus milestone label in parentheses).
 * Kept separate from `timeline-pid-template.ts` so client components can import without Node `crypto`.
 */
export const PID_TEMPLATE_PLACEHOLDER_NOTE =
  "Template seed — placeholder dates; edit or re-log with real business dates when known.";
