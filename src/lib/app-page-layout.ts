/**
 * Shared horizontal layout for app chrome and standard pages — matches `MainNavHeader`
 * (`max-w-[1400px]`, `px-6`). Compose with your own vertical padding and flex direction.
 */
export const appContentWidthClass = "mx-auto w-full min-w-0 max-w-[1400px] px-6";
