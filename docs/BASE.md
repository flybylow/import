# Knowledge Base

This file stores persistent project conventions and lessons learned.

## Core Rule 001 - Documentation Location

- Never place documentation files in the project root.
- Always place documentation in the `docs/` folder.
- Keep lessons in Markdown files and organize them by topic.

## Update Routine

- Add new lessons as soon as we learn them.
- Keep entries short, clear, and practical.
- Review and refresh old entries periodically so guidance stays current.

## Notes Log

- 2026-04-07: **System-wide checkup (BIM ↔ materials ↔ timeline ↔ PID)** — Consolidated review + test run log: **`docs/system-wide-checkup-2026-04-07.md`**. Companion data-flow diagram + ID glossary: **`docs/data-flow-bim-materials-timeline-pid.md`**.
- 2026-04-07: **Events system — documents → timeline** — Document uploads / `data/` artefacts join the audit log when ingest emits the matching `timeline:eventAction` (same vocabulary as PID milestones; one sort key). **`docs/timeline-first-and-document-matching.md`** § Events system; **`docs/timeline-event-taxonomy.md`** intro; **`src/lib/timeline-events-vocab.ts`** file comment.
- 2026-04-07: **Deliveries · PID tab — phase bands** — Dossier milestone cards are grouped **0–1** / **2–3** / **4–9** in **`src/components/DeliveriesPidPanel.tsx`**; rationale (data, actors, flow): **`docs/deliveries-pid-tab-subdivisions.md`**.
- 2026-04-07: **Unified construction lifecycle (Tabulas)** — Stakeholder Phase 0–9 + capture summary: **`docs/unified-construction-lifecycle-tabulas.md`**. Planned structured material episodes: **`docs/material-event-schema.md`**. Deliveries canonical `?tab=`: **`specification` (default when `tab` omitted — URL is normalized)** | `ingest` | `pid` — UI labels: **Bestek** · **Leveringsbon · werf** · **PID**. Aliases: `bestek`/`spec`→specification; `flow`/`leveringsbon`/`delivery`/`werf`→ingest; `lifecycle`/`process`→pid; `bestekFiche=1`→`specificationFiche=1` (`src/lib/deliveries-tabs.ts`, `npm run test:deliveries-tabs`). Timeline: **`pid_reference_milestone`** + `timeline:pidMilestoneKey` (allowlist **`src/lib/timeline-pid-milestones.ts`**; **`npm run test:timeline-pid-milestones`**). **Empty PID view:** Deliveries → PID → **Append full PID template** or **`POST /api/timeline/seed-pid-template`** (optional `force`). PID / spec deep link: `/deliveries?tab=specification&specificationFiche=1`.
- 2026-04-07: **PID reference lifecycle → timeline** — Notion-style Phase 0–9 narrative mapped to current vs proposed `timeline:eventAction`. Spec **`docs/pid-lifecycle-timeline-events.md`**.
- 2026-04-07: **Timeline-first + document matching** — Evidence order in `/timeline` is **`timeline:timestamp`**. Stakeholder docs map to vocabulary: **leveringsbon** → `delivery_document_added` (ingest uses document **`date`** when parseable), **werfverslag** → `site_report_added` via `POST /api/timeline`. Spec **`docs/timeline-first-and-document-matching.md`**; module **`src/lib/timeline-document-matching.ts`**. Dashboard lanes vs roadmap three lanes: **`docs/workflow-readiness.md`** (engineering **phase 0** = element groups JSON, not Belgian process “phase 0”).
- 2026-04-06: **Workflow traceability readiness** — **`/workflow?step=dashboard`** + **`GET /api/workflow/readiness`**: three-lane checklist (technical / audit timeline / bestek package + compliance hints); optional **`data/<projectId>-workflow-readiness-sidecar.json`** for project-specific rows. Spec **`docs/workflow-readiness.md`**. Main nav **Dashboard**; workflow page **3 · Dashboard** pill + **Go to dashboard** link on steps 1–2. **`npm run test:workflow-readiness`**.
- 2026-04-06: **Roadmap snapshot** — **`docs/roadmap-milestones.md`**: shipped vs near-term vs later, named milestone IDs, three-lane diagram (pipeline vs audit timeline vs reference lifecycle); links to PID plan, BASE, stakeholder brief. Refresh monthly or per release.
- 2026-04-06: **Building viewer — ghost, stress, Highlighter** — Internal spec **`docs/building-ifc-viewer-opacity-highlighting.md`**: `FragmentOpacityIntent`, `fragmentsOpacityStressRef` (memory overflow → reload), chunked `setOpacity`, `ghostQueueRef` + nav-focus gate, `bimFocus` vs `select`, construction-only-visible mode, BCF snapshot vs mesh. **`docs/PRD-browser-3d-ifc-and-vr.md`** §4.9 points here.
- 2026-04-06: **Dynamic run vs `*-translated.ttl`** — **`/workflow`** (and **`POST /api/run-example`** → enrich → **`POST /api/kb`**) does **not** run **`POST /api/translate`**, so **`data/<projectId>-translated.ttl`** is usually **absent**; that file is an **optional** intermediate from the separate Translate step. Material→EPD linking for passports/calculate is in **`-kb.ttl`**. Pipeline trace lists Translate for debugging; dashboard labels that row **not written (optional)**. See also **`docs/bim-to-kg-journey.md`** (optional artifact).
- 2026-04-06: **Bestek — Autofill + quantity** — Deliveries → Bestek: **Autofill** beside “shown · total” fills visible rows (`computeBestekAutofillDraft` in **`src/lib/bestek/bestek-autofill-client.ts`**: NL labels, Art.#, unit/qty). Bindings add **`article_quantity`**; timeline **`timeline:bestekArticleQuantity`**. Materials catalog API includes **`declaredUnit`** per row for unit hints.
- 2026-04-06: **Bestek Auto-match — partition slug** — **`suggestedMaterialSlugForBestekGroup`** in **`src/lib/bestek/ifc-type-material-defaults.ts`**: for partitioned IFC types (`IfcCovering` today), Auto-match uses phase-0 **partition** (e.g. `gipsplafond` → `gypsum_plaster`, `gevelisolatie` → `insulation_generic`) before falling back to coarse IFC default. **`npm run test:ifc-type-material-defaults`**.
- 2026-04-06: **Bestek bindings — article unit** — `data/<projectId>-bestek-bindings.json`: optional **`article_unit`** (m², m³, st, kg, …) next to existing **`article_number`**; timeline audit **`timeline:bestekArticleUnit`**. UI: Deliveries → Bestek table **Unit** column.
- 2026-04-06: **Bestek NL article template** — Flemish material-article layout (Materiaalbeschrijving, Uitvoering, Opmetingsstaat): **`docs/bestek-material-template-nl.md`** + mapping to bindings / dictionary / translations.
- 2026-04-06: **Material label translations (beside KB)** — **`src/data/material-label-translations.json`**: NL (`nl-BE`) display strings keyed by **`epdSlug`** (same as `bim:epd-{slug}` / dictionary). Duplicate slugs use **`standardNameEn`** to match the right `material-dictionary.json` row (e.g. both **`timber`**). Loader **`src/lib/material-label-translations.ts`**; **`nlLabelForDictionaryEntry`**. Bestek KB vocabulary merges NL as **`dictionary-nl`** suggestions (same **`materialSlug`**). Not emitted in Turtle yet; future: contractor pack / per-architect. **`npm run test:material-labels`** validates slugs against the dictionary.
- 2026-04-06: **Bestek Dictionary** — **Deliveries** tab: IFC-type groups, architect bindings (optional **`material_slug`**), contractor coupling. **`/deliveries/match-materials`** — ultra-simple IFC group → material + notes only; **`GET /api/deliveries/bestek/matching-form`** + **`POST …/save-matching`** → **`data/<projectId>-bestek-material-matching.json`**; smart defaults in **`src/lib/bestek/ifc-type-material-defaults.ts`**. Other data: `-phase0-element-groups.json`, `-bestek-bindings.json`, `-product-coupling.json`. APIs also: **`…/materials/catalog`**, **`…/materials/search`**, **`…/bindings`**, **`…/product-coupling`**. Timeline: `timeline:bestekMaterialSlug` on bestek binding events.
- 2026-04-04: **Timeline — schedule + BCF + UI window** — MS Project XML → `construction_schedule_task` (`npm run import:schependomlaan-schedule`); BCFzip → `bcf_coordination_event` (`npm run import:schependomlaan-bcf`); optional **`data/<projectId>-schedule-links.json`** for task→material/expressId. **`/timeline`**: `asOfFrom` / `asOfUntil` (legacy `asOf` = until-only), source + kind filters, month-grouped event log. Spec: **`docs/timeline-schedule-integration.md`**.
- 2026-04-04: **Schependomlaan Synchro native schedule** — Canonical copy for tooling: **`docs/DataSetArch/As Planned models/Synchro/Synchro project Schependomlaan.sp`** (Synchro project; pairs with planning XML under `docs/DataSetArch/Planning/XML/` and as-planned IFC exports in the same *As Planned models* folder). See **`docs/DataSetArch/README.md`** §Planning and as-planned models. **Parsing `.sp` is deferred** — use MS Project XML for automation (same doc).
- 2026-04-04: **Deliveries ingest → `data/`** — Optional `projectId` on `POST /api/deliveries/ingest` with `recordTimelineEvent` / `appendDeliveriesTurtle` appends **`delivery_document_added`** to `data/<projectId>-timeline.ttl` and/or Turtle to `data/<projectId>-deliveries.ttl`. No SQL in-repo; external DB as edge service: **`docs/deliveries-importer-integration.md`**. UI: `/deliveries`.
- 2026-04-04: **3D authoring integration handoff** — Briefing for reports on **Revit / SketchUp / IFC round-trip** and carrying **material + CO₂** into tools architects use: **`docs/integration-3d-authoring-tools-report.md`** (what we ship today vs export options, risks, phased recommendations).
- 2026-04-04: **Timeline + EPCIS ingest** — Suppliers can `POST /api/timeline/epcis` with GS1-style `ObjectEvent` JSON; events append to `data/<projectId>-timeline.ttl` with `timeline:source` `epcis`, `epcis_supply_chain_event`, optional `materialReference` / `confidence`. Manual form uses `source` `form`. UI: `/timeline` → **New event** → EPCIS panel + **Copy curl**. Spec: **`docs/timeline-epcis-integration.md`**.
- 2026-04-06: **Timeline event taxonomy** — All `timeline:eventAction` values + time vs role/provenance lanes + EPCIS type/bizStep/field inventory: **`docs/timeline-event-taxonomy.md`** (EPCIS detail still in **`docs/timeline-epcis-integration.md`**).
- 2026-04-04: **IfcMaterialList `|` + mixed wood/concrete** — **`material-norm`**: `loofhout` → timber. **`layer2-translate`**: if label has `|` and normalized text has both **timber** and **reinforced + concrete**, skip **`concrete_reinforced_in_situ`** / **`concrete_general`** so **`timber`** can win (e.g. `305 loofhout | … gewapend beton …`). Dictionary **`routing-2026-04-04-composite-materiallist`**: drop standalone **`gewapend`** pattern; add **`loofhout`** to timber patterns. Rebuild KB after pull.
- 2026-04-03: **Passports abstract 3D** — **`BimViewer3D`** is back on **`/bim?view=passports`**: URL **`expressId`** selects a box; camera focuses after load (selected row is always injected into the 600-cap slice when present in the batch). Not in batch → amber notice + link to **Building**. Full IFC isolation by id → That Open **`@thatopen/components`** / upstream **[docs.thatopen.com](https://docs.thatopen.com/intro)** (future: `getItemsIdsWithGeometry`-style APIs + per-item bbox in **`BuildingIfcViewer`**).
- 2026-04-03: **`/bim?view=3dtest`** — Uses **`BuildingIfcViewer`** (same as the Building tab): **`data/<projectId>.ifc`** via **`/api/file`** or **`/ifc/test.ifc`**. Deep link from passports (“3D sample”) loads the real IFC mesh, not a GLTF placeholder.
- 2026-04-03: **PID digitization (Postinterventiedossier)** — product tiers, gaps vs current pipeline, phased roadmap (compliance slice → deliveries → audit timeline → export), and open questions: **`docs/pid-digitization-plan.md`**. Not legal advice; rule engine and B-EPD fire-field inventory still TBD.
- 2026-04-03: **Compliance pilot (Phase A)** — `GET /api/kb/status` element passports include **`ifcFireRating`** (IFC Pset → `ont:fireRating`). Pure rule helpers in **`src/lib/compliance-pilot.ts`**; UI block **Compliance (pilot)** on **`/calculate`** (pass/warn/fail, expandable lists). Checklist for data/legal: **`docs/compliance-data-checklist.md`**.
- 2026-04-03: **Compliance run audit trail** — **Record run to TTL** on `/calculate` appends `compliance:ComplianceRun` to **`data/<projectId>-compliance-events.ttl`** (`src/lib/compliance-run-turtle.ts`, **`POST /api/compliance-run`**). See **`docs/compliance-run-ttl.md`**.
- 2026-04-03: **`/kb?projectId=…`** — **`useProjectId`** (`src/lib/useProjectId.ts`) reads **`projectId` from `useSearchParams()`** on every navigation (not only first mount), with **`Suspense`** in **`src/app/layout.js`**. Root layout nav still uses **`/kb`** without a query; bookmarked **`/kb?projectId=example`** selects that project for Phase 2 API calls and UI.
- 2026-04-03: **Compliance failures → fix links** — pilot rows include **`missingEpdMaterials` / `lcaBlockedMaterials`**, **What this means**, and links to **`/kb?focusMaterialId=`**, **Sources**, **`/bim?view=passports&expressId=`** (`CompliancePilotPanel`). **`/bim`** tabs: **`view=building` | `passports` | `inspect`** (URL-synced). **Passports** uses **`BimPassportWorkspace`**: loads **`kbStatusPassportsUrl`** (`src/lib/phase4-passports.ts`), then mounts list + **`BimViewer3D`** + panel. **Inspect** = same API, JSON preview. **`router.replace`** on element pick (passports). **Calculate** dashboard links to **`/kb`**, **`/bim?view=passports`**, **`/bim?view=inspect`**. See **`docs/PRD-browser-3d-ifc-and-vr.md`** §2.2.
- 2026-04-03: **`BimViewer3D` sizing** — Follow [three.js manual — Responsive design](https://threejs.org/manual/en/responsive.html): canvas **`display:block; width/height:100%`**, **`renderer.setSize(bufW, bufH, false)`** (drawing buffer + DPR; do not let three.js set canvas CSS px). **Inner mount** uses inline **`minHeight: min(45dvh, 22rem)`** so parent **`min-h-0`** never collapses the viewport. **`ResizeObserver`** + **double `rAF`**. **Passport grid**: **`xl:max-h-full`**, **`xl:grid-rows-[minmax(0,1fr)]`**. **Smoke:** **`/bim/abstract-smoke`**. Scene effect depends only on **`prepared`**.
- 2026-04-03: **Dictionary `routing-2026-04-03-kozijn-gipsplaat`** — **`material-norm`**: `gipsplaat` / `gipsvezelplaat` before bare `gips` (fixes `26 Gipsplaat`). **`material-dictionary.json`**: `gypsum_plaster` patterns + **`aluminium_window_frame`** with **`kbobUuid`** `9F6858EE-…` for `IFC_kozijn_*` → normalized `aluminium window frame`. Rebuild KB after pull.
- 2026-03-24: **Phase 3 — 0 kgCO2e debugging** — step through **one** IFC material at a time (KB `bim:material-*` / `bim:epd-*`, then `data/<projectId>-calc-latest.json` `calculationNote` + quantities). Runbook: **`docs/calc-zero-debug-one-material.md`**.
- 2026-03-24: **Codebase health** — fixed `tsc` errors (calculate grouped rows: `materialIds` on Map type; That Open camera `change` listener typings). Run **`npx tsc --noEmit`** alongside lint. Follow-ups: validate **`projectId`** on every API that writes under `data/` (same as `isSafeProjectId` on clean-pipeline), dedupe RDF helpers across `kb` routes. See **`docs/codebase-health-2026-03-24.md`**.
- 2026-03-23: Initialized knowledge base with Rule 001 (all docs live in `docs/`).
- 2026-03-23: Added PRD-derived project knowledge in `docs/PRD-SUMMARY.md` (architecture, modules, phases, and success criteria).
- 2026-03-23: Phase 2 Step 1 enrichment stores all IFC `BaseQuantities` in `bim:qty-*` nodes (no-loss / Option B) and links them via `ont:hasIfcQuantity` so UI preview can display numbers and later steps can compute carbon reliably.
- 2026-03-23: Phase 2 Step 2 Translate MVP generates EPD nodes and links them via `ont:hasEPD` using a local dictionary (`src/data/material-dictionary.json`) since external EPD APIs are not yet authenticated/integrated.
- 2026-03-23: Added Phase 2 Knowledge Base screen (`/kb`) that loads the last enriched TTL and builds `data/<projectId>-kb.ttl` by matching materials to EPD data (MVP dictionary matcher for now).
- 2026-03-23: Added manual matching UI + endpoint for unmatched KB materials (`POST /api/kb/override`) so we can gap-fill by selecting an EPD from `epdCatalog`.
- 2026-03-23: Added Phase 3 Calculate prep screen (`/calculate`) and `GET /api/kb/status` so we can show what’s matched vs missing before running carbon calculation.
- 2026-03-23: Standardized UX naming to `Phase 2 - Link`; top navigation now points Phase 2 to `/kb`, and `/calculate` is the Phase 3 handoff page while calculation remains a stub.
- 2026-03-23: Phase 2 can match against versioned **KBOB** and **ICE (Educational)** Turtle snapshots under `data/sources/` (see `config.json`, `npm run import:sources`, and `docs/data-sources-kbob-ice.md`). Dictionary matches still take precedence; bulk sources are consulted in config order.
- 2026-03-23: Phase 3 (`/calculate`) now includes a selectable calculation scope list (only rows with quantity records), optional full trace-table toggle, and a `Calculate` button that sends selected rows to `POST /api/calculate`.
- 2026-03-23: `POST /api/calculate` now persists both JSON and graph artifacts: `data/<projectId>-calc-latest.json` and `data/<projectId>-calc.ttl`.
- 2026-03-23: Reusable toast notifications are provided app-wide via `ToastProvider` and `useToast` for action confirmations across phases.
- 2026-03-23: `projectId` is now shared via `useProjectId` and editable only in Phase 1; Phase 2/3 show it as read-only via shared `ProjectIdField`.
- 2026-03-23: Documented KG expansion entry points (BE/NL/EU EPD & LCA sources, links, licensing caveats) in `docs/kg-expansion-sources-benelux-eu.md`.
- 2026-03-23: Phase 2 translate (`layer2-translate.ts`) **hydrates** dictionary-matched EPD nodes from **KBOB/ICE** source TTL when possible. See `docs/kg-dictionary-source-hydration.md`.
- 2026-03-24: **Matching engine**: `material-dictionary.json` is **routing-only** (no invented GWP/density). Source matches emit **`ont:sourceMatchScore`** (raw overlap). Materials without a source hit use `dictionary-no-lca` / `dictionary-no-lca-data` instead of fake LCA literals.
- 2026-03-24: **`POST /api/calculate`** uses **real IFC quantity totals** (from the trace / `compactQuantities`) and **GWP + declared unit + density** read from **`data/<projectId>-kb.ttl`** per `epdSlug` (`src/lib/kb-read-epd.ts`). Requires KB file present; no stub `epdFactor()` curve.
- 2026-03-24: Documented **reset / flush** of generated pipeline files: `docs/reset-and-clean.md`, `npm run clean:pipeline` (`scripts/clean-pipeline.js`), **`POST /api/clean-pipeline`**, and **Reset pipeline data** on Phase 1 (`/`).
- 2026-03-23: **Sources contract** documented in `docs/sources-contract.md` (snapshot folders, `config.json` schema, reports, reproducibility). `docs/data-sources-kbob-ice.md` links to it. **`/sources`** UI: import, enable/disable, reorder, refresh. **`/kb`**: per-source version line + warnings when TTL missing or source disabled.
- 2026-03-24: **`material-dictionary.json`** is loaded from disk on each KB build (`loadMaterialDictionaryFromDisk` in `layer2-translate.ts`) so edits apply without restarting the dev server or relying on a stale Next.js JSON bundle. **`POST /api/kb`** returns **`buildMeta`** (enriched file mtime/size, dictionary version + mtime, KB build time) for debugging “same EPD coverage numbers”.
- 2026-03-25: **EPD coverage tuning** — shared **`src/lib/material-norm.ts`** maps common **NL/BE IFC layer names** (e.g. `isolatie`→`insulation`, `buitenblad`→`brick masonry…`, `HSB-element`→`timber frame…`) so ICE/KBOB `matchText` overlap improves; **`MIN_SOURCE_SCORE`** in `source-match.ts` lowered **9→7**; dictionary **`routing-2026-03-25`** adds **`masonry_brick`** and **`insulation_generic`** routes. **`npm run report:epd`** runs a dry-run translate and prints matched/total (example project: **~360/473** vs **~263/473** before — verify with your `projectId`).
- 2026-03-26: **Mapping QA** — **`docs/cursor-review-mapping.md`**: material→EPD tables are **KB rows**, not a file checklist; **Yes/No** = LCA literals present or not. Dictionary **`routing-2026-03-26`**: **insulation** split (**PIR / EPS / XPS / glass wool** before generic); **`zinc_work`** vs **steel** (remove **`zink`** from steel); **`glaswol`→`glass wool`** in **`material-norm.ts`**.
- 2026-03-27: **`docs/trace-table-for-dictionary.md`** — how to use Calculate **full trace table** columns (Material, EPD, LCA in KB, Elements, Qty) to prioritise dictionary / norm updates; dictionary **`routing-2026-03-27`** adds **screed / ceramic / natural stone / gypsum / glass railing** routes, **AAC** ordered before **masonry** (cellenbeton vs **metselwerk**), **steel** patterns (**metalstud**, **stripstaal**).
- 2026-03-27: **`docs/reuse-locales-and-sources.md`** — **reuse vs custom**: pipeline and sources are reusable; **dictionary + material-norm** are **locale/project-specific** (extend patterns per language/market; no automatic translation).
- 2026-03-27: **`docs/kg-triple-completeness.md`** — what “100% complete” can mean; **triple checklist** (material / EPD / calc); gap is usually **missing instances + literals**, not unnamed predicates; **EN 15804 module-level** data not modeled yet.
- 2026-03-24: **Product passport (per element)** — `GET /api/kb/status` includes **`elementPassports`** (preview), **`elementPassportTotal`**, **`elementPassportsLimit`**, **`elementPassportElementsTotal`**, **`elementPassportsUniqueName`**. Query **`elementPassportsLimit`** (default 80, max 300). By default (**`elementPassportsUniqueName`** omitted or not `false`) each row is **one representative element per distinct `schema:name`** (case-insensitive; first `bim:element-*` by id); **`elementPassportTotal`** counts **unique names**; **`elementPassportElementsTotal`** is total elements. Set **`elementPassportsUniqueName=false`** for one row per element (names may repeat). **Phase 3** `/calculate` shows the **Product passport** section using this data.
- 2026-03-24: **KB graph views** — `/kb` graph area: **`KbGraphVisualization`** tabs: **Force graph** (canvas + inspector), **Grouped by EPD** (filterable list), **Hub outline (SVG)** (hub→EPD spokes, circle size ∝ material count).
- 2026-03-24: **BIM → KG journey** — in-app **`/pipeline`** page (timeline + `projectId`-aware paths) and **`docs/bim-to-kg-journey.md`** (full flow + artifact table). Link **Pipeline** lives in the **avatar (A) menu** next to **Sources** (not in the main phase nav).
- 2026-03-31: **Pipeline debug trace** — **`GET /api/pipeline/trace?projectId=…`** returns on-disk phase artifacts + dictionary + sources; UI entry **`Debug trace`** lives in the **avatar (A) admin menu** (`PipelineTraceDebugButton`). See **`docs/bim-to-kg-journey.md`** (Debug section).
- 2026-03-24: **KB force graph** — `react-force-graph-2d` uses a **shadow canvas** (indexed colors) for clicks; when **pointer hit discs overlap**, only the **last-drawn** node wins. Dense **green EPD** rings + tight zoom made most EPDs unclickable. **Fix:** EPD layout uses a **wider** ring factor + **split rings** (max 18 EPDs/ring), **EPD nodes drawn last** (on top of materials), **smaller/capped** `nodePointerAreaPaint` radius, **`enableNodeDrag={false}`** for fixed layout.
- 2026-03-31: **`material-norm.ts`** — **`kalkzandsteen`** (NL) → **`kalksandstein`** + calcium silicate brick tokens; **`metselwerk`** → masonry/brickwork — improves KBOB/ICE **`matchText`** overlap for layers like *“03 Metselwerk - kalkzandsteen C”* (rebuild KB after norm changes: **`npm run build:kb`** or **Build KB** on `/kb`). 
- 2026-03-31: **Dictionary `kbobUuid`** (e.g. **`kalkzandsteen`** → Hunziker row in KBOB TTL) — when overlap score is below **`MIN_SOURCE_SCORE`**, Phase 2 still resolves the **KBOB** EPD by UUID so **`dictionary_exact+no-lca`** / missing GWP does not happen if the snapshot is present. See **`docs/kg-dictionary-source-hydration.md`**. Rebuild KB after **`material-dictionary.json`** changes.
- 2026-03-31: **Dictionary `routing-2026-03-31`** — **wandtegels** → ceramic; **verlaagd plafond / scheidingswand / brandwering / promatect** → gypsum (tighter **kalkzandsteen** patterns — removed bare `calcium silicate`); **platsica / plastic sheet** → `plastic_sheet`; **lucht frame** → steel; **schoonloopmat** via norm + routing; **Lege arcering** and **nameless IfcMaterialList** skipped in **`layer2-translate`** (no auto EPD). **`matchMaterialToDictionary`** now uses the same **join-then-normalize** combined string as source scoring. **Source-only** match is skipped for placeholder labels (`material 12345`, very short) so accidental overlaps (e.g. AAC) don’t attach.
- 2026-03-23: **Phase A (calculate gate)** — `POST /api/calculate` returns **422** if any selection uses an EPD with **`ont:epdDataProvenance`** placeholder routing (`dictionary-no-lca-data` / `dictionary-placeholder`) or **missing `ont:gwpPerUnit`**. `calculationBlockedReason` in `src/lib/kb-read-epd.ts`; `/api/kb/status` adds **`lcaReady`** per EPD; Phase 3 lists only **LCA-ready** rows with quantities. **`layer2-translate`**: source-only new EPDs get **`ont:epdDataProvenance`** = `source-import`. See `docs/kg-dictionary-source-hydration.md`.
- 2026-03-23: **`epd-hub` source** — hand-picked programme EPDs (EPD Hub, Environdec, B-EPD, …) as Turtle under `data/sources/epd-hub/`, `"type": "epd-hub"` in `config.json`, parse base `https://tabulas.eu/sources/epdhub#`. Slug = fragment without `entry-` prefix (e.g. `hub-4855`). See `docs/epd-handpick-ttl.md`.
- 2026-03-30: **`b-epd-be` source (Belgian B-EPD)** — extracted JSON (`data/sources/B-EPD/epd_data_*.json`, EN 15804+A2 indicators) is converted to Turtle by **`npm run import:b-epd`** (`scripts/import-b-epd.js` → `data/sources/B-EPD/b-epd-2026-03-28.ttl` + `.report.json`). Register in **`config.json`** with `"type": "b-epd-be"` (parse base `https://tabulas.eu/sources/b-epd-be#`; BIM slug prefix **`b-epd-`**). **GWP** for matching uses **`GWP_total_A1-A3`** when present, else **sum of `GWP_total_A1` + A2 + A3** (cradle-to-gate modules). **Declared unit** is taken from readable PDF text when possible; for corrupted fields, **filename heuristics** assign `1 m2`, `1 m3`, or `kg` (per-tonne text → GWP scaled to **per kg**). Rows without a resolvable unit or GWP are **skipped** (see report `skipSamples`). **Source choice:** strict **config order priority**; matcher takes the **first** source with score ≥ `MIN_SOURCE_SCORE` (see `pickFirstOrderedSourceMatch` in `source-match.ts`). **`/kb` “Matched by source”** uses **`ont:source` on the linked EPD** (dataset, e.g. `b-epd-be-source`) when LCA is present; **`dictionary-no-lca`** rows still use the material’s `ont:source`. Re-run **Phase 2 Link** after regenerating the TTL.
